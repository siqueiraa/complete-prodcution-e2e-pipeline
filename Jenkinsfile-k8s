pipeline {
  agent {
    kubernetes {
      yamlFile 'kaniko-builder.yaml'
      defaultContainer 'jnlp'
      credentialsId 'k8s'
      namespace 'jenkins'
      serverUrl 'https://kubernetes.default.svc'
    }
  }

  environment {
    APP_NAME = "complete-prodcution-e2e-pipeline"
    OCI_SERVER = "zrh.ocir.io"
    OCI_NAMESPACE = "zrexacpiqwde"
    OCI_REPO = "trader_go"
    OCI_IMAGE_NAME = "${OCI_SERVER}/${OCI_NAMESPACE}/${APP_NAME}:latest"
  }

  stages {
    stage("Cleanup Workspace") {
      steps {
        cleanWs()
      }
    }

    stage("Checkout from SCM") {
      steps {
        git branch: 'main', credentialsId: 'github-credentials', url: 'https://github.com/siqueiraa/complete-prodcution-e2e-pipeline'
      }
    }


    stage('Verify Kubernetes Connection') {
      withKubeConfig([credentialsId: 'k8s', serverUrl: 'https://152.67.85.76:6443']) {
      sh 'kubectl get pods -n jenkins'
    }
  }

    stage('Build & Push with Kaniko') {
      steps {
        container(name: 'kaniko', shell: '/busybox/sh') {
          sh '''#!/busybox/sh
              /kaniko/executor --dockerfile="${WORKSPACE}/Dockerfile" --context="${WORKSPACE}" --destination="${OCI_IMAGE_NAME}"
            '''
        }
      }
    }
  }
}
